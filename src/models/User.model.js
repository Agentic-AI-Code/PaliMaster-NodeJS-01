// src/models/User.model.js
const TABLE_NAME = 'users';
// Basic schema idea (actual schema will be in migrations)
// { id, username, email, password_hash, xp, level, streak_count, created_at, last_login }
module.exports = { TABLE_NAME };
